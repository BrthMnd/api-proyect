// const router = require("express").Router();
// const {
//   StatusController,
// } = require("../../controllers/Offers/status.controllers");
// const status = new StatusController();

// router.get("/", status.getStatus.bind());
// router.get("/:id", status.getIdStatus.bind());
// router.post("/", status.postStatus.bind());
// router.put("/:id", status.putStatus.bind());

// router.delete("/:id", status.deleteStatus.bind());

// module.exports = router;
